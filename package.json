{
  "name": "alireza-ajax-search",
  "version": "2.2.0",
  "description": "WordPress Ajax Search plugin with Tailwind CSS and esbuild bundle pipeline",
  "main": "alireza-ajax-search.php",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/src/input.css -o ./assets/css/alireza-search.css --minify",
    "build:js": "node esbuild.config.js",
    "build": "npm run build:css && npm run build:js",
    "watch:css": "tailwindcss -i ./assets/css/src/input.css -o ./assets/css/alireza-search.css --watch",
    "watch:js": "node esbuild.config.js --watch",
    "dev": "npm run build && npm run watch:js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tailwindcss": "^3.4.3"
  }
}
